const builder = require('electron-builder');

builder.build({
    config: {
        'appId': 'com.usausa_nurse.unacast',
        'win':{
            'target': {
                'target': 'zip',
                'arch': [
                    'x64',
                    'ia32',
                ]
            }
        }
    }
});
